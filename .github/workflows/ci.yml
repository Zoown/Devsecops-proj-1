name: CI Tests

on:
  pull_request:
    branches: ["main", "apartments"]
  push:
    branches: ["main", "apartments"]

jobs:
  test:
    name: CI Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Placeholder step
        run: echo "CI workflow placeholder — no tests yet"
